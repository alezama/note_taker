{
  "name": "noteflow",
  "version": "1.0.0",
  "description": "AI-powered note-taking app with SQLite backend",
  "main": "server.js",
  "scripts": {
    "start": "node --experimental-sqlite server.js",
    "dev": "nodemon --exec 'node --experimental-sqlite' server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.1",
    "express": "^4.18.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
